# Python 3.12 + CUDA 12.1 stack.
# Install PyTorch first from the cu121 wheel index, then the rest:
#   pip install torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
#   pip install -e submodules/depth-diff-gaussian-rasterization
#   pip install -e submodules/simple-knn

torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

numpy
matplotlib
lpips
plyfile
imageio
imageio-ffmpeg
open3d>=0.19.0
torchmetrics>=1.4
tensorboard
tqdm
Pillow
